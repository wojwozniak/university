const addNumbers2 = (a: number, b = 3) => {
  return a+b;
}
// TODO: fix it
let a: any;

const addNumbers3 = (a: number | string , b: number | string) => {
  return Number(a) + Number(b);
}

addNumbers2(1)

let b;

//b.length
